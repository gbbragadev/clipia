__pycache__/
*.pyc
.env
.env.local
.admin-credentials.local
storage/
*.egg-info/
dist/
.venv/
.venv312/
.pytest_cache/
reference_voices/*.wav
.gstack/
.next/
.playwright-mcp/
.remember/
finalTEMP_MPY_wvf_snd.mp4

# Local agent / QA artifacts
.claude/
evidence/
excalidraw.log

# Windows reserved device name recriado por redirects `> nul` no git bash (quebra Turbopack/Tailwind)
nul
**/nul

# Superpowers brainstorm (visual companion mockups)
.superpowers/

# claude-mem: instrucoes privadas por-diretorio, nunca versionadas (poluiam git status em todo worktree)
CLAUDE.local.md
**/CLAUDE.local.md

# pytest basetemp / temporarios (davam "Permission denied" no git status)
.pytest-basetemp-*/
pytestbasetemp-current/
.tmp/

# Screenshots de smoke/evidencia das sessoes de agente (ficam so no disco)
/*.png
/frontend/smoke-*.png

# Evidencias de sessao (screenshots de smoke/QA na raiz do frontend)
frontend/*.png

# Artefatos pesados de marketing/mockups das sessoes (ficam so no disco, nao versionar)
marketing/concept3-anatomia/
marketing/promo-10s/
marketing/veo-prompts/
samples veo/
dashboard-mockup.html
frontend/public/dashboard-mockup.html
*.ass
.ground-first/
