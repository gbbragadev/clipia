/** Botão canônico do app (primary/secondary/ghost, tamanhos, ícones).
 * A implementação vive no design system da landing — este re-export existe para
 * que o app logado importe de '@/components/ui' sem acoplar em paths da landing. */
export { Button } from '@/components/landing/ui/Button'
